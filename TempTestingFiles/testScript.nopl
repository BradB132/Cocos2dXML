
//get the texture that we care about
Pointer scene = scene;
Pointer sprite = scene.lolTex;
Pointer spinSpeed = scene.spinSpeed;

//spin it a little bit each frame
sprite.setRotation(sprite.rotation+spinSpeed.value);

spinSpeed.setValue(spinSpeed.value*0.999f);

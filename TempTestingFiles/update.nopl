
//get the texture that we care about
Pointer scene = scene;
Pointer hero = scene.hero;

if(!scene.isGripping.value)
{
	hero.setPosition(hero.positionX+scene.horizontalJumpSpeed.value, hero.positionY-2);
}
